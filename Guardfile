guard 'rspec' do
  watch(%r{^spec})
  # watch(%r{^lib/cyoi})    { |m| "spec" }
  watch(%r{^lib/cyoi/cli/(?:|provider_)address})    { |m| "spec/integration/cli/address" }
  watch(%r{^lib/cyoi/cli/(?:|provider_)key_pair})    { |m| "spec/integration/cli/key_pair" }
end

